<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.gbif</groupId>
    <artifactId>motherpom</artifactId>
    <version>3</version>
  </parent>

  <artifactId>gbif-httputils</artifactId>
  <version>0.0.1</version>
  <packaging>jar</packaging>

  <name>GBIF HttpUtils</name>
  <description>Library of common HTTP utilities</description>
  <url>http://sites.gbif.org/common-resources/gbif-httputils</url>
  
  <properties>
    <junit.version>4.8.1</junit.version>
    <commons-lang.version>2.5</commons-lang.version>
    <commons-io.version>1.4</commons-io.version>
    <slf4j.version>1.6.1</slf4j.version>
	<httpclient.version>4.0.3</httpclient.version>
	<guice.version>2.0</guice.version>
  </properties>

  <scm>
    <connection>scm:svn:http://gbif-common-resources.googlecode.com/svn/gbif-httputils/tags/gbif-httputils-0.0.1</connection>
    <developerConnection>scm:svn:https://gbif-common-resources.googlecode.com/svn/gbif-httputils/tags/gbif-httputils-0.0.1</developerConnection>
    <url>http://code.google.com/p/gbif-common-resources/source/browse/#svn/gbif-httputils/tags/gbif-httputils-0.0.1</url>
  </scm>

  <issueManagement>
    <system>Google Code</system>
    <url>http://code.google.com/p/gbif-common-resources/issues/list</url>
  </issueManagement>

  <distributionManagement>
    <site>
      <id>gbif-site</id>
      <name>Site for the common-demo project</name>
      <url>scp://tools.gbif.org:/var/www/html/sites/common-resources/gbif-httputils</url>
    </site>
  </distributionManagement>

  <repositories>
    <repository>
      <id>gbif-all</id>
      <url>http://repository.gbif.org/content/groups/gbif</url>
    </repository>
  </repositories>

  <dependencies>
		<!-- Guice provides the code based (not XML based) injection dependency framework -->
		<dependency>
			<groupId>com.google.inject</groupId>
			<artifactId>guice</artifactId>
			<version>${guice.version}</version>
			<exclusions>

				<!-- superceded by guava and causing exceptions under tomcat6 -->
				<!--
				java.lang.NoSuchMethodError com.google.common.collect.ImmutableList.copyOf(Ljava/util/Collection;)
				-->
				<exclusion>
					<groupId>com.google.code.google-collections</groupId>
					<artifactId>google-collect</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		
		<!-- native java io cannot handle redirects -->
		<dependency>
			<groupId>org.apache.httpcomponents</groupId>
			<artifactId>httpclient</artifactId>
			<version>${httpclient.version}</version>
		</dependency>
		
		<!-- file utils -->
		<dependency>
			<groupId>commons-io</groupId>
			<artifactId>commons-io</artifactId>
			<version>${commons-io.version}</version>
		</dependency>
		
		<!-- Commons lang provides utilities for (e.g.) simple hashcode building -->
		<dependency>
			<groupId>commons-lang</groupId>
			<artifactId>commons-lang</artifactId>
			<version>${commons-lang.version}</version>
		</dependency>
		
		<!-- testing -->
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>  
		
		 <!-- Logging -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
  </dependencies>

</project>
